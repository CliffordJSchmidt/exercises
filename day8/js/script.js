//alert(Hello from the script.js file);

var value = null, 
    type = null;

// var value = null, type = null;    or write as 
//  var value = null, 
//  type = null;

/*
value = '123';  // we assign '123' into variable value
type = typeof value; // we get the type of variable value and assign it into variable type
console.log(type);  // we output the variable 'type' (containing the type of variable 'value)
*/



/*
'123'
123
'123 Times Square'
12.3
true
'true'
[1, 2, 3]
{ foo: 'bar' }
document
*/

/*
value = 123;
type = typeof value;
console.log(type);


value = '123 Times Square';
type = typeof value;
console.log(type);

value = 12.3;
type = typeof value;
console.log(type);


value = true;
type = typeof value;
console.log(type);

value = 'true';
type = typeof value;
console.log(type);

value = [1, 2, 3];  //notation for array
type = typeof value;
console.log(type);

value = { foo: 'bar' };  // notation for object
type = typeof value;
console.log(type);

value = document;
type = typeof value;
console.log(type);
*/

var message_element = document.getElementById('message');
console.log(message_element);
console.log(typeof message_element);


/*  need jquery loaded.... everything inside is ran only when entire webpage is loaded

$(function() {
    
    
});

*/